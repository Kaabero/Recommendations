{
  "name": "backendpsql",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "cross-env NODE_ENV=production node dist/index.js",
    "build": "tsc",
    "dev": "cross-env NODE_ENV=development ts-node-dev --respawn --transpile-only index.ts",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "lint": "eslint .",
    "start:test": "cross-env NODE_ENV=teste2e ts-node-dev --respawn --transpile-only index.ts",
    "migration:down": "ts-node util/rollback.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cross-env": "^10.1.0",
    "umzug": "^3.8.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/sequelize": "^6.12.0",
    "jest": "^30.2.0",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
